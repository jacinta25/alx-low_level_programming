#include <stdio.h>
/**
 * main - prints the name of a program
 * @argc:unused parameter
 * @argv: contains the arguements to be passed
 *
 * Return: always 0
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
